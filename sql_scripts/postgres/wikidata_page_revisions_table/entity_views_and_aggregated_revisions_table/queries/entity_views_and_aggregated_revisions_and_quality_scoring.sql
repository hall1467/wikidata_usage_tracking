\copy (SELECT aggregations.entity_id, aggregations.page_views, predictions.prediction, SUBSTRING(cast(predictions.monthly_timestamp as text) from 3 for 4) FROM entity_views_and_aggregated_revisions AS aggregations INNER JOIN (SELECT DISTINCT title, rev_id, prediction, monthly_timestamp FROM monthly_item_quality)   AS predictions ON predictions.title = aggregations.entity_id) TO '/export/scratch2/wmf/wbc_entity_usage/usage_results/sql_queries/entity_views_and_aggregated_revisions/entity_views_and_aggregated_revisions_and_quality_scoring.tsv';